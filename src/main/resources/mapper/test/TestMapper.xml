<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.anything.odoc.test.dao.TestDao">

        <select id="selectName" resultType="String">
            SELECT
                name
            FROM TEST_TB
            limit 1
        </select>

</mapper>
